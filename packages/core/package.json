{
  "name": "@devicehub/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript API client layer for DeviceHub's auth-service/user-service/chat-service (issue #219) — no UI, no WebRTC; reused as-is by apps/web and apps/mobile.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "vitest": "^2.1.4"
  }
}
