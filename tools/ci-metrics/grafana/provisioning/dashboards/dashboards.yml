apiVersion: 1

providers:
  - name: ci-metrics
    orgId: 1
    folder: CI
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
