// Placeholder — the real login/register/OTP forms are issue #265.
// Exists here only so App.tsx's routing (and ProtectedRoute's redirect
// target) has somewhere real to render.

export function LoginPage() {
  return <p>Login page — issue #265.</p>;
}
