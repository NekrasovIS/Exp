// Placeholder — the real communities/channels sidebar and chat view are
// issues #266/#267 (and #268 for friends/DMs). Exists here only so
// ProtectedRoute has a real element to render once signed in.

export function HomePage() {
  return <p>Signed in — communities/channels sidebar lands in issue #266.</p>;
}
