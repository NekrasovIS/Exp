{
  "name": "devicehub-clients",
  "private": true,
  "description": "Monorepo for DeviceHub's thin web/mobile clients and their shared TypeScript API layer (issue #219, part of #214). The C++ backend (auth-service/user-service/chat-service) is unaffected and unrelated to this workspace.",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "eslint .",
    "format": "prettier --check \"apps/**/*.{ts,tsx}\" \"packages/**/*.ts\" \"*.{mjs,json}\""
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  }
}
